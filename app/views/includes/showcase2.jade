.showcase-wrapper
  .showcase
    each val, i in showcase2
      a.card-wrapper(href='/about/#{val.url}')
        .showcase-item.smooth-scroll(class='showcase-item-#{i}')
          .show-card
            .circle
              img(src='images/#{val.img}')
            .show-card-right
              h2= val.head
              p= val.body
              .show-card-bottom-wrapper
                .show-card-bottom
                  hr
                  .show-card-footer
                    i.fa.fa-chevron-down
.clearfix
